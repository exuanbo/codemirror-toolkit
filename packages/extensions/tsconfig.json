{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Modules
    "rootDir": "./src",
    // Emit
    "outDir": "./dist",
    // Language and Environment
    "lib": ["ESNext", "DOM", "DOM.Iterable"]
  },
  "include": ["./src"],
  "references": [{ "path": "../utils" }]
}
