{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Modules
    "rootDir": "./src",
    // Emit
    "outDir": "./dist",
    // Language and Environment
    "lib": ["DOM", "DOM.Iterable", "ESNext"]
  },
  "include": ["./src"],
  "references": [{ "path": "../utils" }]
}
